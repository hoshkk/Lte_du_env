# Spectrum Check v2.2.1 — 빠른 현장 설정

이 버전은 장비 리버스 / 불요파 두 버튼과 사용자 설정 저장을 추가했습니다.
기존 수동 설정, 마커, CSV, RBW/VBW, Ref/Offset, Channel Power를 유지합니다.

## 처음 사용할 때
1. 측정 설정 → FREQ / SPAN에서 현장 RX 대역 또는 주파수와 Span을 확인합니다.
2. 장비 리버스 또는 불요파을 누릅니다. 아직 저장값이 없으면 현재 주파수와 Span을 유지합니다.
3. 연결 상태와 이득/Offset을 확인하고 실측 시작을 누릅니다. 버튼은 설정만 적용하며 수신을 자동 시작하지 않습니다.

| 초기 설정 | 장비 리버스 | 불요파 |
|---|---|---|
| RBW 목표 | 100 kHz | 10 kHz |
| VBW | 1 kHz (소프트웨어) | OFF |
| 튜너 AGC | OFF | OFF |
| 수동 이득 | 1/10 | 1/10 |
| Ref Level / Scale | 0 / 10 dB/div | 0 / 10 dB/div |
| Offset | 0 dB | 0 dB |
| Channel Power | ON | OFF |
| Integration BW | 현재 값과 Span 중 작은 값 | 현재 값과 Span 중 작은 값 |
| Max Hold | OFF | ON |
| DC 제거 | OFF | OFF |

이 값들은 검증된 최적값이 아닌, 사용자가 조정할 수 있는 시작값입니다. 수신이 약하면 이득을 단계적으로 조정합니다. 이득을 높이면 강한 신호에 의한 과부하가 생길 수 있으며 클리핑 경고가 없더라도 과부하가 없다고 확정할 수 없습니다.

## 현장에 맞춰 저장
측정 설정에서 주파수, Span, RBW/VBW, Ref, Offset, 이득, AGC, DC 제거, Channel Power/Integration BW를 조절하고 적용합니다. Max Hold 상태도 원하는 대로 맞춥니다.
빠른 설정 저장/관리 → 현재 값을 해당 모드에 저장을 누릅니다.
이후 해당 모드 버튼을 누르면 저장된 주파수를 포함한 전체 설정을 불러옵니다. 각 모드에 한 개씩 저장하며 앱 재실행 후에도 유지됩니다. 앱 삭제/데이터 삭제 시 사라집니다. 대역별로 여러 슬롯을 저장하는 기능은 없습니다.
저장값 초기화는 저장된 프리셋만 지우며, 현재 측정 설정을 즉시 변경하지 않습니다. 다음 모드 선택 시 시작값을 적용합니다.
프리셋 전환 시 수신을 정지하고 이전 그래프·마커·Max Hold를 비웁니다. 실측 시작으로 재개합니다.
두 번째 줄의 모드 버튼 아래에는 Ref / Offset / Scale / RBW / VBW 설정값을 바로 표시합니다. 최근 적용 이력과 설정 적용 안내 문구는 표시하지 않습니다. 연결 오류·정지·수신·DEMO 상태는 그래프 아래에 표시합니다.

## 장비에 연결하기 전
TX 출력이나 TX가 함께 존재할 수 있는 공용 안테나 포트에 직접 연결하지 마세요. 연결할 RX 모니터 포트의 규격·감쇠량, DC 유무, SDR 허용 입력을 확인하고 필요한 보호 부품을 사용합니다. 수동 이득 최소값은 감쇠기나 과입력 보호가 아닙니다.
장비/포트/케이블 정보 없이 Offset을 추정해 넣지 않습니다. 초기값은 0입니다. 앱은 입력한 Offset을 원본 dBFS에 더하며, 감쇠량 입력만으로 dBm 교정이 되지 않습니다. 포트나 케이블이 달라지면 저장값을 다시 확인합니다.

## 측정 해석
불요파은 상대 변화 관측용입니다. 위치를 옮기면 피크 초기화를 누르세요. Max Hold는 이전 위치의 최대값도 남기며 위치를 자동 기록하지 않습니다.
넓은 Span은 순차 스윕이므로 간헐 신호를 놓칠 수 있습니다. 의심 주파수를 찾으면 Span을 약 1.5 MHz 등으로 좁혀 관측하고 현장 조건에 맞게 저장하세요.
RBW는 FFT로 가능한 가까운 실제 값을 적용하며 목표값/적용값이 다를 수 있습니다. VBW는 소프트웨어 평활입니다.
장비 RX 모드는 전체 역방향 경로, 장비 고장, 외부 간섭, PIMD의 원인을 자동 판정하지 않습니다. 측정 조건을 고정한 상대 비교를 돕는 기능입니다.

## 설치 및 검증
v2.1.0과 동일한 보관 서명키로 빌드합니다. v2.1.0 사용자는 업데이트 설치할 수 있습니다. 이전 v2.0.1은 서명이 달라 제거 후 설치가 필요할 수 있습니다.
SDR Driver는 그대로 사용합니다. 실제 V4/휴대폰 연결 및 RF 교정은 사용자 현장에서 확인해야 합니다.
